---
type: Method
id: loss-fn
title: Loss Function
---

# Loss Function

A loss function scores how wrong a prediction is. It is what
[[Supervised ML]] minimises.

(`[[Supervised ML]]` resolves by **alias**. NOTE: this Method has **no
timestamp**, which the manifest says Method `requires` — expect a warning.)
