% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/okf.R
\name{okf_ingest}
\alias{okf_ingest}
\title{Ingest an OKF bundle into a DuckDB catalog.}
\usage{
okf_ingest(
  root,
  db_path = ":memory:",
  ingested_at = NULL,
  bundle_id = NULL,
  source_kind = "dir",
  subdir = NULL,
  branch = NULL,
  incremental = FALSE
)
}
\arguments{
\item{root}{A bundle directory path, a git URL, a tar/zip path or URL, or a
bundle list from [okf_read()]. Non-directory sources are fetched via
[okf_fetch()] and cleaned up afterwards.}

\item{db_path}{DuckDB path; defaults to in-memory `":memory:"`.}

\item{ingested_at}{Optional ISO-8601 timestamp; defaults to the current time.}

\item{bundle_id}{Optional stable bundle id.}

\item{source_kind}{How the bundle was obtained (e.g. `"dir"`); auto-set for
fetched sources.}

\item{subdir}{Optional bundle path within a cloned/extracted source.}

\item{branch}{Optional git branch or tag (git sources only).}

\item{incremental}{Only rewrite concepts whose `content_hash` changed since a
prior ingest of the same bundle into `db_path` (added/removed handled too);
links and validation are always recomputed (they are graph-global). Falls
back to a full load if the bundle is not already present. The `summary`
then includes `changed`/`added`/`removed`/`cached` counts.}
}
\value{
A list with the open `con`, the `bundle_id`, and a `summary`
  (counts, conformance, link totals). The caller owns/closes `con`.
}
\description{
Reads, validates, and loads the bundle into the `okf_bundle`, `okf_concept`,
`okf_link`, and `okf_validation` tables of a (file or in-memory) DuckDB
database.
}
